
<%# messy cos my focus was elsewhere %>
<p class="text-center text-xl w-screen"><%= current_user.user_name %>
<div class="text-center ">
<% @game_rooms.each do |game_room| %>
<%= link_to "Join Room",  join_match_game_room_path(game_room, {user_id:current_user}) %>



<% end %>
